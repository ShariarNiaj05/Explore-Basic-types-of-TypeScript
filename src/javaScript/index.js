"use strict";
const course = "Next level web development...";
console.log(course);
const nodemonTest = "testing if nodemon working or not....";
console.log(nodemonTest);
// Primitive Types
/***
 * string
 * number
 * boolean
 * null
 * undefined
 *
 */
// As we're going to Declare the type is's called "Explicit Type"
const myName = "Shariar";
const rollNumber = 101;
const isAdmin = true;
