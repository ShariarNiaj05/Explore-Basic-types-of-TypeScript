const course: string = "Next level web development...";
console.log(course);

const nodemonTest: string = "testing if nodemon working or not....";
console.log(nodemonTest);

// Primitive Types

/***
 * string
 * number
 * boolean
 * null
 * undefined
 *
 */

// As we're going to Declare the type is's called "Explicit Type"
const myName: string = "Shariar";
const rollNumber: number = 101;
const isAdmin: boolean = true;
